name: Fetch MLB Player Data

on:
  workflow_dispatch:

jobs:
  fetch-players:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch MLB player data
        run: python3 scripts/fetch_mlb.py --mode players

      - name: Check generated files
        run: |
          echo "=== Generated files ==="
          ls -la data/mlb/players/ || echo "No files generated"
          echo "File count: $(ls data/mlb/players/*.json 2>/dev/null | wc -l)"

      - name: Commit data files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/mlb/
          git diff --staged --quiet || git commit -m "data: fetch MLB player JSONs ($(date +%Y-%m-%d))"
          git push
